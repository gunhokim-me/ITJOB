<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com">
	
  <select id="selectAllCom_user" resultType="com_UsersVo">
    SELECT * FROM com_users
  </select>
  
  <select id="selectCom_user" resultType="com_UsersVo" parameterType="String">
  	SELECT * FROM com_users WHERE com_id = #{com_id} and pass = #{pass}
  </select>
  
<!--   <select id="selectCom_user" resultType="userVo" parameterType="pageVo"> -->
<!--   	SELECT * -->
<!-- 	FROM -->
<!-- 		(SELECT ROWNUM rn, a.* -->
<!-- 	FROM -->
<!--    		 (SELECT * -->
<!--    			 FROM com_users -->
<!--    			 ORDER BY com_id)a -->
<!--    	 WHERE ROWNUM &lt;= #{page} * #{pageSize}) -->
<!-- 	WHERE rn BETWEEN (#{page}-1) * #{pageSize}+1 AND #{page} * #{pageSize} -->
<!--   </select> -->
  
  
  
</mapper>

